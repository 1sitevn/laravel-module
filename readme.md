# Laravel artisan make:module command

This package adds the ```php artisan make:module command```, allowing 
you to:

> Generate a model, migration, controller, routes and model factory in a single easy to use command.